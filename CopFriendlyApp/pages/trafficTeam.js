import { useState } from "react";
import { Text, View } from "react-native";

export default MedicalTeam = ({ navigation }) => {
    return (
        <View>
            <Text>Traffic Team</Text>
        </View>
    );
}